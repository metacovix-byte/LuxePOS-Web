name: Release (Windows + macOS + auto-updater)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target universal-apple-darwin'
            target: 'universal-apple-darwin'
          - platform: 'windows-latest'
            args: ''
            target: 'x64'

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Cache Rust deps
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.toml') }}

      - name: Install npm deps
        run: npm ci || npm install

      - name: Sync HTML to dist-web
        shell: bash
        run: |
          cp luxepos-final.html dist-web/luxepos-final.html
          cp luxepos-final.html dist-web/index.html

      - name: Build Tauri (signed for updater)
        id: tauri
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          # Pas de password car la clé a été générée sans password
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'LuxePOS ${{ github.ref_name }}'
          releaseBody: 'Voir CHANGELOG.md'
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
          updaterJsonPreferNsis: true

      - name: Debug — list bundle artifacts
        if: always()
        shell: bash
        run: |
          echo "=== Bundle directory contents ==="
          find src-tauri/target -name "*.sig" -o -name "*.exe" -o -name "*.dmg" -o -name "*.app.tar.gz" 2>/dev/null || echo "Nothing found"
          echo "=== Signing key length ==="
          if [ -n "$TAURI_SIGNING_PRIVATE_KEY" ]; then
            echo "Key is set (length: ${#TAURI_SIGNING_PRIVATE_KEY})"
          else
            echo "❌ Key is NOT set"
          fi
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
